import { SafeAreaView, Text } from "react-native";

export default function Register({navigation}) {
    return(
        <SafeAreaView>
            <Text>Register ini cok</Text>
        </SafeAreaView>
    )
}